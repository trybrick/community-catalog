PencilBlue:
  image: brickinc/brickpb
  links:
    - mongo:mongo
    - Redis:redis
  restart: always
  tty: true
  stdin_open: true
  volumes:
    - pencilblue-datavolume:/app
  labels:
    io.rancher.sidekicks: pencilblue-datavolume
    io.rancher.container.hostname_override: container_name
    # Make jenkins only run containers on hosts with a dock=1 label
    io.rancher.scheduler.affinity:host_label: dock=1
  environment:
    PB_REDIS_HOST=${PB_REDIS_HOST}
    PB_SITE_PORT=${PB_SITE_PORT}
  ports:
    - "80:80"

pencilblue-datavolume:
  net: none
  labels:
    io.rancher.container.hostname_override: container_name
    io.rancher.container.start_once: true
  volumes:
    - /app
  entrypoint: /bin/true
  image: busybox

Redis:
  image: redis
  tty: true
  stdin_open: true
  labels:
    io.rancher.container.hostname_override: container_name
